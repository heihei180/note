# 中缀函数

https://zhuanlan.zhihu.com/p/656209298

定义：
```kotlin

infix fun Int.add(x: Int): Int {
    return this + x
}
```

调用：

```kotlin
//这里的 add 就是一个中缀函数。
// 等同于 5.add(3)
val result = 5 add 3 
```

使用限制
- 中缀函数必须是成员函数或扩展函数。
- 中缀函数必须只有一个参数。
- 参数不能是可变参数（varargs）。

# 可变参数